# IntDog 当前架构

[English](DESIGN.md) · [用户指南](README.zh-CN.md)

## 产品边界

IntDog 是本地优先的桌面行业情报工作台。它建立开放世界的行业知识体系，持续发现来源、产业链、实体、事件与前沿方向，并生成带证据状态的研究产物。模型输出默认是待复核草稿，不是已确认事实或投资建议。

正式产品采用 Electron + React + FastAPI + SQLite。三个桌面平台共享业务源码，分别生成原生安装包。行业数据、数据库、日志和生成产物只保存在用户本机，并被 Git 排除。

## 运行与依赖方向

```text
Electron：窗口、生命周期、系统安全存储
  └─ React：唯一正式用户界面
      └─ 会话保护的 localhost FastAPI
          ├─ intdog_core：SQLite、Schema、证据、任务和审计
          ├─ Search：来源连接器、检索、研究和报告
          └─ Agent/Provider adapters：本机 CLI、API、MCP、任务包
```

- SQLite 管理知识记录、任务状态、对话、调度和工作流设置继承。
- 本机 Agent 绑定保存在 `_settings/agent_profiles.json`；加密 API 配置由 Electron 管理。浏览器存储保存界面偏好，不是 Provider 设置的权威来源。
- Markdown、报告元数据文件和便携 HTML 是本地产物，读取时结合 SQLite 记录。行业导出和恢复必须保留这一文件边界。
- Electron 不写业务事实；React 没有文件系统或凭据访问权。
- API Key 只进入操作系统安全存储，经一次性匿名管道传给本地 sidecar。

## 规范存储与恢复

`intdog.sqlite3` 是唯一可写业务数据库。旧 `daily`、`weekly` 命令分别转接到 `crawl-daily`、`crawl-weekly`；旧归档调用通过统一服务导入，不再维护 `intelligence.db` 和独立索引。每周采集生成数据与任务包；`generate-period --kind weekly --provider ...` 才生成模型撰写的草稿。显式迁移保留输入文件。

可选 JSON 状态不存在时可以使用默认值；已存在但损坏或不可读时必须报错，不能作为空集合覆盖。兼容 JSON 是物化视图：数据库写入标记 dirty，对账负责修复。独立采集来源失败时其他来源可以继续，但缺少必需输入会阻止下游报告，部分完成不推进成功采集的时间边界。调度轮询失败显示在系统健康状态与日志中。

初始化先发现来源候选并等待人工审核。采用来源需要发布者与所属机构的引用依据，以及受保护的网址访问检查。来源、产业链、实体通过结构门槛后仍是草稿，不是已核实事实。相同来源的已完成检查点直接复用，不重写知识；新一轮研究补充现有知识。在无法可靠判定生成归属时，初始化不得清空现有实体、关系或产业链节点，人工记录与笔记必须保留。

知识区分行业、规范来源与发布者、文档、实体、时态关系与事件、主张以及支持/反驳/限定证据。覆盖维度包括地域、子领域、产业链位置、实体/来源/事件类型与时间，固定数量不证明完整性。用户水平只改变解释深度，不缩减底层知识范围。SQLite 连接在成功和失败时均需关闭，迁移保留旧输入。Lab 产物不可变且经过哈希校验；产业链边的证据数来自实际证据链接。情景结果是启发式暴露度，不是因果概率。详见 [Intelligence Lab](DomainIntelSearch/INTELLIGENCE_LAB.md)。

产品不提供邮件推送；支持 CLI、桌面工作台和只读 MCP。

## 设置继承

所有行业默认共享任务设置和 Agent/Provider。只保存用户真正修改的覆盖项：

```text
系统默认 → 全局设置 → 全局任务覆盖 → 行业覆盖 → 行业内任务覆盖
```

全局修改只影响仍在继承的行业。每个覆盖项均显示来源范围，并可恢复为“继承全局”。工作流设置只保存 Provider、执行方式和周期产物模式；密钥不进入 SQLite，由桌面安全存储单独管理。

## Agent 与 Provider

能力目录是 Agent/API 的唯一清单。所有本机 Agent 统一经过候选发现、路径规范化、有界指纹、版本探测、认证探测和能力判定。大型合法 CLI 不能因固定 64 MiB 上限被误判；执行前仍重新校验绑定。

默认连接边界是同一操作系统、同一用户账户。发现流程合并桌面进程 `PATH`、有限的常见安装目录和用户通过系统文件选择器明确选择的命令文件，不扫描主目录或整块磁盘，也不默认跨 Windows/WSL。已验证的手动绑定写入本地 `_settings/agent_profiles.json`，供初始化、前台任务和后台调度共享；文件改变后必须重新诊断。

- Codex CLI 与 Claude Code只有在稳定的非交互适配器和公开登录探针通过后才能直接执行。
- DeepSeek Harness、Work Buddy、Qwen Code、CodeBuddy、Kimi、Gemini CLI、OpenCode及后续 Agent 使用同一诊断管线；没有直接适配器时只提供 MCP、任务包或结果导入。
- GUI 正在运行不等于可调用，未知 Agent 不自动获得直接执行权限。
- 就绪状态由版本与公开认证探针确定；用户主动点击的真实连接测试还必须完成一次带固定标记的最小非交互调用。静态 MCP 配置存在不能替代真实 Agent 验收。
- OpenAI、DeepSeek、Qwen、Azure OpenAI及兼容 API 必须显式配置；远程端点必须使用 HTTPS。

## 一键工作流与进度

知识结构、行业初始化、周期产物、深度研究与 Intelligence Lab 均提供文档化默认值。主按钮点击一次即执行；高级设置可以事前展开，但不再要求先生成任务包再二次确认。

后台仍使用持久任务和租约。当前页面显示语义阶段、已用时间、心跳、代表性计数和产物链接。只有存在可计算总量时才显示百分比；否则显示“不确定进度”，不得用伪造百分比掩盖任务包或空产物。

## 来源、论文与采集预算

完整来源目录非破坏性保留。重点监控池根据边际覆盖价值选择来源：权威性、地区、主题/产业链节点、独立发布者、有效产出和更新频率。每类通常保留 3–10 个重点来源；有新增覆盖价值时可继续扩展，不以数量证明完整性。

相对上一基线：

- 来源发现与检索候选预算提高 50%；
- 一般采集条目上限提高 50%；
- 论文采集目标提高 100%。

论文不仅补充已知行业关键词，还用于发现潜在新子领域、跨学科迁移、尚未产业化方向和最前沿技术。探索条目必须标记为候选方向，不能自动写成产业事实。扩容服从去重、独立发布者、时间窗口、速率限制和成品质量门；如果没有有效新增，允许提前停止。

## 证据、漂移与 Prompt

事实、主张、关系、来源、文档、Story、任务和审核使用稳定 ID。链接可达不等于支持断言；事实准入还需要证据定位、语义支持、数字/单位一致性和声明类型要求的独立佐证。

首次运行或没有同版本基线时，漂移状态是“未发现漂移；数据不足以判断趋势”，不产生警告。完整指标只在详情中显示。

来源发现与行业初始化复用来源 Prompt。报告、研究助手和 Agent 模块各自拥有适合其输出的模板。报告与 `execute-tasks` 共用 `artifact_quality`，检查正文、引用、占位符、重复与结构化证据。研究章节不是新闻条目；逐条日期与来源要求适用于明确标记为 `briefing` 的产物，不套用到每个研究标题。通过检查仍是草稿，不是正式事实。任务包区分草稿、部分产物和跳过任务；任务包未完整执行时 CLI 返回非零退出码。

原生 Agent 会话创建时发送完整近期上下文，续聊或成功恢复会话后只发送新增用户轮次。无状态 CLI/API 调用（包括原生会话降级）发送完整近期上下文。未知发布日期不能满足标题去重的时间窗口；规范 URL 与充分内容一致仍是独立去重依据。

## 状态与错误处理边界

工作台从设置 API 读取生效的 Provider。读取失败时显示错误，不自动选择替代 Provider。切换行业会重新挂载行业页面和对话。切换产物或实体选择会取消旧请求，过期响应不能覆盖最新选择。文档锚点不应改变工作台页面。

在 HTTP 响应、用户操作、持久任务结果、事务和进程清理边界处理错误。必要读取失败不能伪装成空结果。保留单来源失败隔离，避免一个不可达网站中止整轮采集。

## 已知限制

- 产物检查验证排版和引用结构，不证明语义支持或行业研究完整。
- 标题去重在缺失发布日期时保留不确定的匹配项。

## 运行保存与会话顺序

每次任务包执行分配独立的本地运行目录。在 Agent 调用前和每个任务产生结果后，原子替换运行清单。Provider 失败时保留已完成结果，标记失败任务，并将后续任务记为未开始；不自动重试。每次运行保留独立产物快照，显式指定的输出路径仍保存最新副本。进程被强制终止时保留最后检查点，不伪造成功；磁盘故障可能阻止继续保存。

在桌面后端进程内，同一行业、同一 Provider 的会话按轮次串行完成历史读取、Agent 调用和回复保存，其他会话互不阻塞。失败会释放锁。这是单后端约定，不提供多进程协调。

## 文档与发行

公开仓库只保留当前、必要、双语对齐的用户指南、架构、来源治理和发行说明。审批包、迭代日志、截图证据、本机路径和旧状态快照不属于产品文档，后续只保存在 Git 忽略的本地工作目录。

测试采用风险驱动的最小集合：设置继承、Agent 诊断、任务状态、来源/论文预算、漂移首日语义、数据不入 Git、Web 构建和桌面契约。真正的冷启动行业验收由用户执行，项目提供可重复入口和清单，不替用户创建行业数据。
